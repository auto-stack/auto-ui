widget counter {
    model {
        var count = 0
    }
    view {
        button("+") {
            onclick: || count = count + 0
        }
        text("Count: {}")
        button("-")
        text("halal")
    }
}