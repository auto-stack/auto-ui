use auto.ui: View, widget, app, center, text

widget Hello {
    // data fields
    msg str

    // view
    fn view() View {
        text(msg) {}
    }
}

// Create an App
app CounterExample {
    center {
        Hello("Hello, World!")
    }
}
