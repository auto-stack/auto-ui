// 简单的计数器组件 - 用于测试动态解释器
// 这是一个最小化的示例，验证解释器能否正确加载和渲染

type Counter is Widget {
    count int = 0

    fn view() {
        center {
            col {
                label("Count: {self.count}") {}
                button("Increment") {
                    onclick: "inc"
                }
                button("Decrement") {
                    onclick: "dec"
                }
            }
        }
    }

    fn on(ev str) {
        if ev == "inc" {
            count = count + 1
        } else if ev == "dec" {
            count = count - 1
        }
    }
}

fn main() {
    app("Counter Test") {
        counter() {}
    }
}
