name: "hello"
story {
    name: "Hello"

    // fields of the model
    field("msg") {
        name: "msg"
        value: "Hello World"
        kind: "SharedString"
    }

    // actual render code for the view
    // need to trans from auto to rust code
    code: "center().child(Label::new(self.msg.clone()))"
}

app {
    title: "Hello Example Title"
}
